let diff = calculateDifference(); // Assume calculateDifference() returns a numerical value

if (diff > 0) {
    // Execute these statements if diff is greater than 0
    console.log("Difference is positive.");
    // Additional statements can be added here
} else {
    // Execute these statements if diff is not greater than 0 (diff <= 0)
    console.log("Difference is not positive.");
}
